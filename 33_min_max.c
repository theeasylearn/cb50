/*
    findout minimum value from  float array 
    steps
    1) create & initialize float array as numbers
    2) create variable min 
    3) store 1st value of array into min variable (assume 1st value is minimum value)
    4) compare 2nd value of array with min variable if min variable has larger value then 2nd value then store 2nd value into min variable 
       if(min>number[1])
            min = number[1]
    5)  if(min>number[2])
            min = number[1]       
    6)  if(min>number[3)
            min = number[1]   
    7)  if(min>number[4)
            min = number[1]         
    write code as per steps
*/
